# Viber Bot - Менеджер 170+ Магазинів

Цей проект є мастер-ботом, який дозволяє адміністраторам відправляти повідомлення відразу у всі 170+ каналів/груп магазинів, використовуючи їхні індивідуальні API-токени, зібрані в одному Excel файлі.

## 🚀 Як це працює?
1. В системі є Головний бот (Master Bot) - з ним спілкується відповідальний менеджер (адміністратор).
2. Керуючий створює 170+ ботів (по 1 на кожен магазин) і додає кожного бота як адміністратора у відповідний канал магазину.
3. Токени цих 170+ ботів зберігаються у файлі `stores.xlsx`.
4. Адміністратор надсилає пост (текст, фото і т.д.) Головному боту в особисті повідомлення.
5. Головний бот запитує підтвердження. Після натискання "✅ Підтвердити", скрипт починає розсилку: він проходить по списку з `stores.xlsx` і відправляє це ж повідомлення через API кожного з 170+ ботів.
6. Вбудована затримка (1.5 сек між відправками) захищає від блокування (ban/rate limit) від Viber.
7. Після завершення Головний бот надсилає звіт про успішні та неуспішні відправки.

## ⚙️ Налаштування для локального запуску та Railway

### 1. Змінні середовища (.env)
Створіть файл `.env` (або скопіюйте з `.env.example`) та заповніть наступні поля:
```env
MASTER_BOT_TOKEN="Токен_головного_бота_який_спілкується_з_адміном"
PORT=3000
WEBHOOK_URL="Ваша_URL_адреса_на_Railway/viber/webhook"
ADMIN_VIBER_IDS="Ваш_Viber_ID,ID_Іншого_Адміна"
```
*(Ваш ID можна дізнатись, якщо написати щось боту з порожнім полем ADMIN_VIBER_IDS - бот відповість "У вас немає доступу... Ваш ID: ...")*

### 2. Файл бази (`stores.xlsx`)
Файл Excel `stores.xlsx` має лежати у корені проекту. Він повинен містити дві обов'язкові колонки (назви не чутливі до регістру і можуть містити інші слова):
- **Назва** (Назва магазину, щоб ідентифікувати його в логах)
- **Токен** (API token бота для цього магазину)

Приклад таблиці:
| Назва | Токен (API ключ) | Місто/Регіон | Менеджер Контакт |
| :--- | :--- | :--- | :--- |
| Магазин Київ Центр | 5623a0f0a1... | Київ | +380991234567 |
| Магазин Львів | 82b8ab09ab... | Львів | +380997654321 |

### 3. Запуск проекту
```bash
npm install
npm start
```

## 🛠 Подальші кроки (для Railway):
1. Завантажте цей код у свій GitHub репозиторій.
2. Підключіть репозиторій до Railway.
3. У Railway додайте в **Variables** всі значення з файлу `.env`. 
4. Отримайте згенерований домен в Railway і вставте його у змінну `WEBHOOK_URL` як `https://ваш-домен.railway.app/viber/webhook`.
5. Після рестарту Railway бот автоматично підключить Webhook до Viber і буде готовий приймати ваші команди!
